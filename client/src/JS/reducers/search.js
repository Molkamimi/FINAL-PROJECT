import { GET_TEXT } from "../const/searchPost";

const intialState = {
  text: "",
};
export const filterReducer = (state = intialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
